
<% if(user){%>
<div class="thumbnail">
	<div class="caption">
		<img src="" alt="">
		<h2>个人资料</h2>
		<% } %>
		<div>
			<% var url = null;
				if(user){
					url = user.head;
				}else{
					url = 'imgs/logo.jpg';
				}%>
			<ul>
				<% if(user) {%>
				<li>账号：<a href="/user?name=<%=user.name%>"><%=user.name %></a></li>
				<li>文章数：</li>
				<% } %>
			</ul>
		</div>
	</div>
</div>

